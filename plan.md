# Planning 阶段交付文档

## 0. 规划范围与目标对齐

本计划严格基于已确认的 Research 结论，目标聚焦以下三类核心指标：
1. 分割质量（主指标）
2. 推理实时性
3. 复杂环境鲁棒性（光照、遮挡、密集重叠、复杂背景）

训练阶段允许并鼓励使用项目原生 LoRA 能力，以提高迭代效率并降低资源开销。
当前阶段不将 ROS 端到端链路与 RGB-D 像素级严格对齐作为本轮实现约束。

前置输入约束（由你提供）：
1. 数据集（路径、划分、标注）
2. conda 环境（Python 与依赖可用）

本轮实施边界：
- 仅尝试 YOLO-Master 分割 n 模型。
- 我负责在你提供的数据与环境上完成训练、评估、推理、导出链路实现与验收。

---

## 1. 本次需求整体实现方案

### 1.1 总体策略
采用“最小入侵、可回归、可迭代”的工程策略，在现有 YOLO-MoE 分割链路上完成苹果二分类语义分割专项落地：
- 以现有分割任务链路为主干（segment trainer/validator/predictor）
- 通过新增数据集配置、训练脚本与评估模块，完成苹果场景能力建设
- 通过 LoRA 与原生训练配置实现低成本迭代
- 通过导出与测速脚本形成部署前验收闭环

### 1.2 交付形态
本轮开发结束后应具备：
- 可复现训练入口（含 baseline 与 LoRA 微调两种模式）
- 可复现评估入口（标准分割指标 + 自定义业务指标）
- 可复现推理与测速入口（全图推理与 ROI 推理）
- 可导出部署工件（ONNX/TensorRT，含 INT8 验证流程）

### 1.3 验收策略
采用“分层验收”：
- 层 1：功能验收（训练/评估/推理链路是否完整可运行）
- 层 2：指标验收（分割质量、实时性、鲁棒性）
- 层 3：工程验收（文档、配置、脚本可复用）

---

## 2. 技术选型说明

### 2.1 模型选型
- 主模型：YOLO-Master v0.1 分割 n 模型
- 本轮固定：仅进行 n 模型训练与评估，不扩展到 s/m/l/x
- 选型理由：
  - 已与项目 MoE 体系一致
  - 已集成 segment 头与训练链路
  - 参数量和算力成本更低，适合快速建立高频迭代闭环

### 2.2 训练策略选型
- 阶段 A：全量微调 baseline（建立稳态基线）
- 阶段 B：LoRA 参数高效微调（场景快速迭代）
- 阶段 C：困难样本回灌再训练（提升鲁棒性）

### 2.3 指标体系选型
语义分割评价指标分为三组，并统一统计全场景与分场景（光照/遮挡/密集重叠/复杂背景）结果。

1. 分割质量主指标（核心验收）
- mIoU（主指标）
  - 定义：按类别 IoU 求平均，二分类下可同时报告前景 IoU 与总体 mIoU。
- 前景像素 Precision、Recall、F1(Dice)
  - Precision = TP / (TP + FP)
  - Recall = TP / (TP + FN)
  - Dice = 2TP / (2TP + FP + FN)
- Boundary F1（边界质量）
  - 在 GT/Pred 边界的容差带内计算边界精确率与召回率，再取 F1。
- 小目标完整度
  - 面向幼果目标，统计可见目标区域覆盖率（按目标实例聚合）。

2. 实时性指标（部署可行性）
- 单帧延迟（ms）：统计 P50/P95
- FPS：在固定输入分辨率下统计稳定吞吐
- 模式维度：全图推理与 ROI 推理分别统计

3. 复杂环境鲁棒性指标
- 分场景性能退化率
  - 定义：相对 normal 场景的指标下降比例（mIoU、Recall、Boundary F1）
- 目标：控制极端场景下主指标退化幅度，避免整果漏检与大面积误分

### 2.4 部署选型
- 导出：ONNX + TensorRT（FP16 与 INT8 两套）
- 推理模式：全图推理 + ROI 推理
- 校验：量化前后精度退化与速度收益联合评估

---

## 3. 模块拆分与职责定义

### 3.1 数据与配置模块
职责：定义苹果二分类数据集、数据划分、类别、增强策略。
- 对接：你提供的数据集与划分
- 新增：项目可用的数据集映射 YAML（必要时）
- 新增：训练配置 YAML（baseline 与 LoRA）
- 输出：可直接调用的训练配置组合

### 3.2 训练编排模块
职责：统一训练入口、参数编排、日志记录、模型保存策略。
- 新增：训练脚本（支持 baseline 和 LoRA）
- 复用：项目原生 segment trainer 与 LoRA 参数
- 输出：可复现实验目录与权重文件

### 3.3 评估与验收模块
职责：在原生指标基础上增加业务指标计算与报告生成。
- 新增：业务指标评估脚本（mIoU、边界、小目标完整度、分场景统计）
- 新增：鲁棒性分组评估配置（光照、遮挡、密集重叠、复杂背景）
- 输出：统一评估报告（csv/json/md）

### 3.4 推理与测速模块
职责：实现线上可用推理接口与性能基准测试。
- 新增：单图/批量/视频推理脚本
- 新增：全图与 ROI 两种测速脚本
- 输出：延迟与吞吐测试报告

### 3.5 导出与部署准备模块
职责：模型导出、量化、推理一致性校验。
- 新增：导出脚本（onnx/engine）
- 新增：INT8 校准与对比评估脚本
- 输出：部署工件与量化对比结论

### 3.6 文档与复现模块
职责：确保团队成员可一键复现实验与验收。
- 新增：使用手册与命令清单
- 新增：参数推荐表与常见问题
- 输出：完整复现文档

---

## 4. 接口设计

### 4.1 训练接口
输入：
- 模型配置路径
- 数据集配置路径
- 训练模式（baseline 或 lora）
- 关键超参（imgsz、batch、epochs、lora_r、lora_alpha 等）

输出：
- 权重文件（best、last）
- 训练日志（loss、标准指标、学习率）
- 训练配置快照

### 4.2 评估接口
输入：
- 权重路径
- 数据集路径或划分名称
- 评估模式（标准指标/业务指标/鲁棒性分组）

输出：
- 标准指标结果
- 业务指标结果
- 分场景统计与可视化图表

### 4.3 推理接口
输入：
- 权重路径
- 图像或视频输入
- 推理模式（full 或 roi）
- 阈值与后处理参数

输出：
- 二值掩码
- 最小外接矩形
- 轮廓坐标
- 时延统计

### 4.4 导出接口
输入：
- 权重路径
- 导出格式（onnx 或 engine）
- 量化配置（fp16/int8）

输出：
- 导出模型文件
- 一致性比对结果
- 延迟与精度对比报告

---

## 5. 数据结构变更设计

### 5.1 数据集配置结构
新增苹果数据集 YAML，字段遵循项目标准：
- path
- train
- val
- test（可选）
- names（仅 1 类 apple，背景隐式）

### 5.2 评估结果结构
新增业务指标结果结构，建议包含：
- run_id
- split
- miou
- fg_precision
- fg_recall
- boundary_error_px_mean
- edge_band_acc
- small_object_completeness
- latency_ms_p50/p95
- fps
- scene_tag（normal/backlight/occlusion/dense/background_complex）

### 5.3 推理输出结构
在原有结果基础上新增规范化字段：
- binary_mask_path 或 binary_mask_array
- contours（多边形点集）
- min_rect（x, y, w, h）
- inference_time_ms

---

## 6. 兼容性处理与工程取舍

### 6.1 兼容性原则
- 不修改公共 API 语义
- 不破坏现有 detect/segment/classify/pose/obb 任务
- 新增能力以脚本和配置扩展为主

### 6.2 工程取舍
- 指标优先级：分割质量 > 鲁棒性 > 实时性
- 当精度与速度冲突时，先保证目标场景分割质量，再通过 ROI/INT8 回收性能
- LoRA 用于快速迭代，不替代 baseline 全量微调基线

---

## 7. 风险点与应对方案

1. 风险：小目标（幼果）掩码破碎或漏检
- 应对：提高输入分辨率策略、强化小目标样本采样、增加边界损失权重实验

2. 风险：复杂光照下前景与背景混淆
- 应对：定向增强（亮度、对比度、阴影、局部过曝）、困难样本回灌

3. 风险：密集果实粘连导致边界分离失败
- 应对：边界指标驱动训练调参，增加接触边界样本比例与后处理轮廓精修

4. 风险：INT8 量化后精度下降超过预期
- 应对：构建覆盖复杂场景的校准集，分层评估并回退 FP16 方案

5. 风险：训练资源受限导致迭代速度慢
- 应对：优先启用 LoRA 与梯度检查点，采用两阶段训练减少全量重训次数

---

## 8. Todo List（顺序、交付物、完成标准）

### T1 数据与配置落地（最高优先级）
- 任务内容：
  - 对接并验证你提供的数据集与 conda 环境
  - 新增/调整苹果分割数据集 YAML（仅在现有格式不兼容时）
  - 新增 baseline 与 LoRA 两套训练配置
- 交付物：
  - 数据与环境对接记录
  - 数据集配置文件（如需）
  - 训练配置文件
- 完成标准：
  - 在你提供 conda 环境中可成功启动训练进程
  - 配置可被训练入口正确加载
  - 类别映射与路径校验通过

### T2 训练入口与实验编排
- 任务内容：
  - 新增统一训练脚本（支持 baseline/lora）
  - 固化日志、权重保存、随机种子
- 交付物：
  - 训练脚本
  - 训练命令模板
- 完成标准：
  - 可完成至少 1 次端到端训练并产出 best 权重

### T3 业务指标评估模块
- 任务内容：
  - 实现 mIoU、前景精确率/召回率、边界偏差、边缘带准确率、小目标完整度计算
  - 输出标准化报告
- 交付物：
  - 评估脚本
  - 评估报告模板
- 完成标准：
  - 给定权重可一键生成完整业务指标报告

### T4 鲁棒性分组评估
- 任务内容：
  - 建立按场景标签分组评估流程（光照、遮挡、密集重叠、复杂背景）
- 交付物：
  - 分组评估脚本
  - 分组统计报告
- 完成标准：
  - 报告包含各场景分组指标及退化幅度

### T5 推理接口与性能基准
- 任务内容：
  - 新增全图与 ROI 推理入口
  - 新增时延/FPS 基准脚本
- 交付物：
  - 推理脚本
  - 性能测试报告
- 完成标准：
  - 可输出掩码、轮廓、最小外接矩形与延迟统计

### T6 导出与量化验证
- 任务内容：
  - 完成 ONNX/TensorRT 导出
  - 完成 INT8 与 FP16 精度-速度对比
- 交付物：
  - 导出脚本
  - 对比报告
- 完成标准：
  - 导出模型可推理且一致性在可接受范围内

### T7 文档收口与复现交付
- 任务内容：
  - 编写运行手册、参数建议、问题排查指南
- 交付物：
  - 项目使用文档
- 完成标准：
  - 新成员可按文档复现训练、评估、推理与导出

---

## 9. 阶段门禁（严格执行）

- 仅当你确认本 plan.md 与 Todo List 完全符合预期后，才进入 Implementation 阶段。
- Implementation 阶段将逐项执行 T1-T7，并在每个 Todo 节点完成后更新本文件状态与变更说明。